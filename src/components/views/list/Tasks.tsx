import List from "./List"; // eslint-disable-line

/** 
 *  ## Vista declarativas de envolvente de tareas
 *  Este componente ayuda a contener la declaración de múltiples tareas de
 *  servidor en la vista de lista del componente {@link List}.
 */ 
const Tasks: React.FC<IACele.UI.GenericInvolverComponent> = ({
    children,
}) => {

    return (children);
};

export default Tasks;
